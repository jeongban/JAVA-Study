package Fight;

public class Knight extends Unit{
    Knight(String name,int attack, int hp ,int MaxHP){
        setName(name);
        setAttack(attack);
        setHp(hp);
        setMaxHP(MaxHP);
    }
}
