package Fight;

public class Villain extends Unit {
    Villain(String name,int attack, int hp ,int MaxHP){
        setName(name);
        setAttack(attack);
        setHp(hp);
        setMaxHP(MaxHP);
    }
}
